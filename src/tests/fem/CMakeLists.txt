macro(ADDPROG name)
    add_executable(${name} ${name}.f90)
    target_link_libraries(${name} hfsolver ${LIBS})
endmacro(ADDPROG)

addprog(poisson2d)
addprog(poisson3d)
addprog(laplace2d_eig)
addprog(schroed2d)
addprog(schroed3d)
